package com.jimliao.application;

public class EmbededTomcat {

	/*public static void main(String[] args) {
		long startTime = System.currentTimeMillis();
		Tomcat tomcat = new Tomcat();
	
		// 设置Tomcat的工作目录:工程根目录/Embedded/Tomcat  
		// TOMCAT_HOME
		tomcat.setBaseDir(System.getProperty("user.dir") + "/Embedded/Tomcat");
		tomcat.setPort(8081);
		tomcat.addWebapp("", System.getProperty("user.dir") + +File.separatorChar + "WebRoot");
		tomcat.enableNaming();//执行这句才能支持JDNI查找  
		tomcat.getConnector().setURIEncoding("utf-8");
	}*/

}
